;; If you change the variable mouse-yank-at-point to a non-nil value,
;; Mouse-2 does not move point; it inserts the text at point,
;; regardless of where you clicked or even which of the frame's
;; windows you clicked on. This variable affects both mouse-yank-primary
;; and mouse-yank-at-click.
(setq mouse-yank-at-point t)
